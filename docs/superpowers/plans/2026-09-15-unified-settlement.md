# Unified Settlement implementation

Approved design: ../specs/2026-09-15-unified-settlement.md.

- [x] Add and test an opt-in, replayable social proposal contract; preserve deterministic old runs and saves.
- [x] Expose a loopback-only optional provider path through the local worker with limits, disabled status, recorded responses and UI controls. Use injected provider responses for tests; no paid requests.
- [x] Adapt the observatory as a contextual workbench driven by the game's shared scene. Add unified navigation and current chapter inspection, preserve library/batch/worker features, redirect /lab into root.
- [x] Verify browser flows, audit changes, run tests/types/lint/build, update documentation and source archive.
